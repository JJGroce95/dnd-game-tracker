#include "newGame.h" // player.h must be in the current directory. or use relative or absolute path to it. e.g #include "include/player.h"

newGame::newGame()
{
    void SetDate();
}

// Date member function
void newGame::SetDate()
{
}